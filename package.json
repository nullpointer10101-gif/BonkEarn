{
  "name": "earn-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix bot && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:bot": "npm run dev --prefix bot",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
